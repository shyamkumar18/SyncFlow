name: syncflow
description: SyncFlow - Personal Finance Platform
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  google_sign_in: ^6.2.0
  provider: ^6.1.0
  intl: ^0.19.0
  fl_chart: ^0.68.0
  url_launcher: ^6.2.0
  share_plus: ^9.0.0
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
